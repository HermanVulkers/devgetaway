import styled from 'styled-components';
import { Check } from 'tabler-icons-react';

export const StagedPhoto = styled.div`
  position: relative;
`;

export const StagedPhotoHeader = styled.div`
  display: flex;
  justify-content: space-between;
  flex-direction: row;
  margin-bottom: 0.2rem;
  column-gap: 0.2rem;
  align-items: center;
`;
